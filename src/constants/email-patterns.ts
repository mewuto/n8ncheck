/**
 * Company email pattern for validating @example.com email addresses
 * Used for emailAddress field validation (user/group)
 */
export const COMPANY_EMAIL_PATTERN = /@example\.com$/i;

/**
 * Company domain pattern for validating example.com domains
 * Used for domain field validation (domain type)
 */
export const COMPANY_DOMAIN_PATTERN = /^example\.com$/i;
